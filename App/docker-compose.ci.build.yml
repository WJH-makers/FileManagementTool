version: '3.4'

services:
  ci-build:
    image: mcr.microsoft.com/dotnet/sdk:8.0
    volumes:
      - .:/src
    working_dir: /src
    command: /bin/bash -c "dotnet restore ./Web/Web.csproj && dotnet publish ./Web/Web.csproj -c Release -o ./obj/Docker/publish"
